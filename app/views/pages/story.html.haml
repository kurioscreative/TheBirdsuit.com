- title "Story - The Bird Suit"
%h2 We Are <strong>The Bird Suit</strong>
.wrapper
  .title-img= image_tag @page.page_img_url
  .two-third-box
    %h2= @page.title
    .bodyContent= @page.body.html_safe
  .story-sidebar
    %h2 Artists

    - @artists.each do |artist| 
      = link_to image_tag(artist.roster_pic_url, 'data-hover' => artist.roster_pic_hover_url), artist_path(artist)

  %br.clear
%h2 Artists
.full-box
  .wrapper
    %ul.artists
      - @artists.each do |artist|
        %li
          = link_to image_tag(artist.roster_pic_url, 'data-hover' => artist.roster_pic_hover_url), resource_path(artist)
          %br
          = link_to artist.name, resource_path(artist)
      %br.clear